"""
练习 9: 综合实战 - 训练你自己的 mini GPT
=========================================

把前面所有练习组合起来，在 Shakespeare 数据上训练一个小 GPT 并生成文本。
这个文件不留 TODO，你需要自己从零写出来。

目标：
1. 加载数据（用 input.txt 或自动下载）
2. 构建字符级 tokenizer
3. 创建 train/val split 和 batch 函数
4. 定义完整 GPT 模型
5. 训练循环（跑到 loss < 1.5 就算成功）
6. 生成一段文本并打印

参考超参数（适合 CPU/小 GPU）：
- batch_size = 32
- block_size = 64
- n_embd = 128
- n_head = 4
- n_layer = 4
- dropout = 0.1
- learning_rate = 3e-4
- max_iters = 3000

提示：
- 如果你前面的练习都做对了，这里就是把它们拼起来
- 可以从前面的文件 import 你写好的模块
- 训练时每 200 步打印一次 loss
- 训练完后生成 500 个字符看看效果

祝你好运！这就是你从"读懂代码"到"自己写代码"的最后一步。
"""

# 从这里开始写你的代码
# 你可以参考前面练习的实现，但尽量不看 nanogpt 源码
